<UserControl x:Class="AdvancedUnitConverter.UI.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32" MaxWidth="700">

            <!-- Hero Banner -->
            <Border Style="{StaticResource ModernCard}" Padding="32" Margin="0,0,0,20">
                <StackPanel HorizontalAlignment="Center">
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Width="64" Height="64" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <Path Data="{StaticResource SwapIcon}" Fill="#FFFFFF" Width="28" Height="28" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <TextBlock Text="Advanced Unit Converter" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Version 2.0.0 (Enterprise Architecture Edition)" FontSize="13" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    <TextBlock Text="A professional, extensible unit conversion system crafted in modern C# and .NET 8 WPF." FontSize="14" Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>

            <!-- Architecture Info -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Architecture &amp; Design Principles" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,12"/>
                    <TextBlock Text="• Clean Architecture separation: Core, Application, Infrastructure, and UI layers" Foreground="{DynamicResource TextBrush}" Margin="0,3"/>
                    <TextBlock Text="• MVVM (Model-View-ViewModel) powered by CommunityToolkit.Mvvm" Foreground="{DynamicResource TextBrush}" Margin="0,3"/>
                    <TextBlock Text="• Fully decoupled, extensible Conversion Engine with modular category converters" Foreground="{DynamicResource TextBrush}" Margin="0,3"/>
                    <TextBlock Text="• High-performance local SQLite storage via Microsoft.Data.Sqlite and Dapper" Foreground="{DynamicResource TextBrush}" Margin="0,3"/>
                    <TextBlock Text="• 14+ comprehensive physical, thermal, data, and scientific unit categories" Foreground="{DynamicResource TextBrush}" Margin="0,3"/>
                </StackPanel>
            </Border>

            <!-- Shortcuts Guide -->
            <Border Style="{StaticResource ModernCard}">
                <StackPanel>
                    <TextBlock Text="Keyboard Shortcuts" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Ctrl + C : Copy conversion result" Foreground="{DynamicResource TextMutedBrush}" Margin="0,3"/>
                            <TextBlock Text="Ctrl + R : Reset values &amp; units" Foreground="{DynamicResource TextMutedBrush}" Margin="0,3"/>
                            <TextBlock Text="Ctrl + F : Open global search" Foreground="{DynamicResource TextMutedBrush}" Margin="0,3"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Ctrl + H : Navigate to History" Foreground="{DynamicResource TextMutedBrush}" Margin="0,3"/>
                            <TextBlock Text="Ctrl + S : Toggle favorite" Foreground="{DynamicResource TextMutedBrush}" Margin="0,3"/>
                            <TextBlock Text="Ctrl + , : Open Settings" Foreground="{DynamicResource TextMutedBrush}" Margin="0,3"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
