<UserControl x:Class="AdvancedUnitConverter.UI.Views.FavoritesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:AdvancedUnitConverter.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Favorite Conversions" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
            <TextBlock Text="Quickly access your pinned unit conversion pairs" FontSize="14" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Empty State -->
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40">
            <StackPanel HorizontalAlignment="Center">
                <Path Data="{StaticResource StarOutlineIcon}" Fill="{DynamicResource TextMutedBrush}" Width="48" Height="48" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock Text="No favorites saved yet" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Click the star icon on any conversion screen to pin it here" FontSize="13" Foreground="{DynamicResource TextMutedBrush}" Margin="0,6,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Favorites Grid -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding FavoriteItems}" Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource InteractiveCard}" Width="260" Margin="0,0,16,16" Padding="16">
                        <Border.InputBindings>
                            <MouseBinding Gesture="LeftClick" 
                                          Command="{Binding DataContext.OpenConversionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                          CommandParameter="{Binding}"/>
                        </Border.InputBindings>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <Border Background="{DynamicResource BgSecondaryBrush}" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Category}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                                </Border>
                                <Button Command="{Binding DataContext.DeleteFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Style="{StaticResource IconButton}" HorizontalAlignment="Right" ToolTip="Remove Favorite">
                                    <Path Data="{StaticResource TrashIcon}" Fill="{DynamicResource DangerBrush}" Width="14" Height="14" Stretch="Uniform"/>
                                </Button>
                            </Grid>

                            <StackPanel Grid.Row="1" Margin="0,14,0,10">
                                <TextBlock Text="{Binding FromUnitName}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock Text="↓ convert to" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,2"/>
                                <TextBlock Text="{Binding ToUnitName}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                            </StackPanel>

                            <Border Grid.Row="2" Background="{DynamicResource BgSecondaryBrush}" CornerRadius="6" Padding="8,6">
                                <TextBlock Text="{Binding FromUnitSymbol, StringFormat='{}{0} → {1}'}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource AccentBrush}"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
